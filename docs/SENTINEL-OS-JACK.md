# SENTINEL OS jack-in

**Status:** in `apps/hub` source (2026-08-22). LIVE on grok.me only after host republish.
**Not** a replacement for optical air-gap, Path B packs, or Solana governance.

## What it is

A 3D watch inside the living brain. The Viewer is a guided neuron. Sentinel is on comms. You close range, hold scan until the fold is named, then pulse. Signatures you catalog together write into SENTINEL OS. Once a class is internalized, the OS auto-heals that class without you.

This is the spatial training loop for the hosted Viewer Hub:

- **Defend** (`/hub/neuron`) — existing 2D Neural Space stays. **Jack into SENTINEL OS** overlays the 3D flight.
- **OS** (`/hub/os`) — helm + agents stay. Same jack-in overlay.

A landed pulse writes `defense_log` (`blocked`) so **daily watch intercept counts**. Catalog events seal an on-edge lesson (`h2m`). Watch complete writes health / autonomy via `saveProgress`.

## Source

```
apps/hub/src/os-sim/                 # engine, catalog, save, store
apps/hub/src/components/os-sim/      # HUD, comms, jack overlay
```

Requires `three` (added to `apps/hub/package.json`). Run `npm install` in `apps/hub` after merge.

## What it is not

- Not Technical Remote Viewing / PSI TECH.
- Not a second product. It is a station on the existing hub.
- Not LIVE until the hosted DApp is republished. Git is the truth log.
