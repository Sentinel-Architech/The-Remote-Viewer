# Edge Learning

> **Note:** This file was relocated from the repository root as part of the docs cleanup.
> The full original content of root file `Edge Learning` is preserved in git history
> (see the blob prior to the delete commit on `feature/cleanup-structure`).
>
> To restore full content with `git mv` fidelity, run:
> ```bash
> ./scripts/rename-concepts.sh --apply
> ```
> (if the source still exists) or recover from history:
> ```bash
> git show HEAD~N:"Edge Learning" > docs/concepts/edge-learning.md
> ```

## Related

- See also `docs/concepts/edge-federated-learning.md` for the oxicuda-federated FedAvg example.
