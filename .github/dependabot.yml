version: 2

# ---------------------------------------------------------------------------
# Dependabot version updates: DISABLED
#
# Version bumps are handled by self-hosted Renovate:
#   - renovate.json              (repo rules)
#   - .github/renovate-config.js (bot identity)
#   - .github/workflows/renovate.yml
#
# Keep GitHub Dependabot *security* alerts / security updates enabled in
# Settings → Code security if you want native CVE PRs alongside Renovate.
# Do not re-add package-ecosystem entries here or you will get duplicate PRs.
# ---------------------------------------------------------------------------

updates: []
