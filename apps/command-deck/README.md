# Command Deck — The Remote Viewer

**In God We Trust.**

Sovereign Command Deck for [The Remote Viewer](https://github.com/Sentinel-Architech/The-Remote-Viewer). Keys stay on the device. Sign-in with X is social, not the key. Native web A–Z. No wallet. No Google identity.

Field: [the-remote-viewer.grok.me](https://the-remote-viewer.grok.me)  
Network Hub: [the-remote-viewer.grok.me](https://the-remote-viewer.grok.me)  
Canonical repo: [Sentinel-Architech/The-Remote-Viewer](https://github.com/Sentinel-Architech/The-Remote-Viewer)

## Clone on any Wi-Fi device

```sh
git clone https://github.com/Sentinel-Architech/The-Remote-Viewer.git
cd The-Remote-Viewer/apps/command-deck
npm install
npm run dev
```

Or this export:

```sh
git clone https://github.com/Sentinel-Architech/theremoteviewer.git
cd theremoteviewer
npm install
npm run dev
```

Install as a PWA from the live Hub, or host this tree yourself.

## Open

1. Gateway first: the eye, then **Red** or **Blue**. Same facts. Two deliveries.
2. Fingerprint (or PIN) unlocks this Viewer.
3. **TRVN social is home** — profile, mesh, vault. Neural Link and God's Eye are perks, never the landing.
4. `/hub` is the map. `/hub/deck?door=neural` is Neural Link.
5. One HUB PIN pairs every device. Keys stay on the device.

## Native ARM64 (Pixel / GrapheneOS / Termux)

The signed node is `remote-viewer-android-arm64.tar.gz` with `sha256sums.txt`. Build on-device with `CARGO_BUILD_JOBS=1`. This web DApp is the Network surface. The ARM64 binary is the Hydra mesh node.

## Decentralized

- Viewer key is Ed25519 on this device. PIN never left the phone.
- Pair is WebRTC. Cloudflare STUN only. Signed max-merge CRDT for the HUB dossier.
- Fork this repo. Host it. Internal Affairs holds any path that leaves the native wire.
- Sentinel Repair labels GitHub issues `repair`. Command Deck never merges.

## Native stack

WebCrypto, WebRTC, WebGL, PWA. Host ICE on Wi-Fi. Zero Google identity. Zero wallet.
