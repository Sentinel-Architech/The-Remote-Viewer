# SENTINEL OS (hub jack-in)

3D neuron flight used by `/hub/neuron` and `/hub/os` via `JackInSession`.

Catalog, engine, and save stay on-device (`localStorage` key `sentinel-os-v1`). Watch intercepts and XP go through the existing hub server functions (`logDefense`, `saveProgress`).

Command Deck (`/hub/deck`, `src/components/command-deck`) is a sibling physics theater with its own catalog (HSV / WNV / Rabies · Emission / Runoff / Worm). Do not merge the two catalogs.
